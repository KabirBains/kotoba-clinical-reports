

The user is asking which files are required in the `report-documents` storage bucket so they can upload them and unblock report generation.

From the previous turn's findings, the `generate-report` edge function loads three template files. Let me confirm by reading the function source so I give exact filenames.
